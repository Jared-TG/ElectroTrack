# ElectroTrack 

Proyecto de monitoreo de consumo eléctrico IoT con React Native y Node.js.

## 🚀 Requisitos Previos
Solo necesitas tener instalado:
- **Docker Desktop** (Corriendo)
- App **Expo Go** en tu celular (Android/iOS)

## 🛠️ Instalación (Paso a paso)

1. **Clonar el repositorio:**
   ```bash(terminal)
   git clone <URL_DEL_REPOSITORIO>
   cd Proyecto-Electrotrack(carpeta en donde lo vas a clonar)

2. Crea un archivo .env y pon LAN_IP = (tu direccion ip local)

3. Arrancar docker y ejecutar el comando

    docker-compose up --build

# --- BASE DE DATOS ---
# esto es temporal 
DB_ROOT_PASS=root
DB_NAME=electrotrack_db
DB_USER=equipo
DB_PASS=equipo_password
